ANTHROPIC_API_KEY=

# Leave QDRANT_URL unset to use Qdrant's embedded local mode (default, no server needed).
# Set it (e.g. http://localhost:6333) once you're running `docker-compose up` for a real server.
# QDRANT_URL=http://localhost:6333
QDRANT_COLLECTION=ph_recycling_law
